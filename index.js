$(document).ready(function() {
	$('.project-image-gallery').slick({
	});
});

//$( ".project-image-gallery-item" ).each(function( index ) { 
//   $( this ).css('margin-top', (360 - $(this).height()) / 2 + 'px' );
// });
